using System;

namespace B8_SOLID
{
    /*
     * SOLID được ghép lại từ 5 chữ viết tắt đầu tiên của 5 nguyên tắc này:
        S is single responsibility principle (SRP) ( là nguyên tắc trách nhiệm duy nhất )
        O stands for open closed principle (OCP)   ( là viết tắt của nguyên tắc đóng mở)
        L Liskov substitution principle (LSP)      ( nguyên tắc thay thế Liskov )
        I interface segregation principle (ISP)    ( nguyên tắc phân tách interface)
        D Dependency injection principle (DIP)     ( nguyên tắc tiêm phụ thuộc)
     
     */
    internal class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}
